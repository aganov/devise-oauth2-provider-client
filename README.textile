h1. Install provider (rails app)

bc. bundle install
bundle exec rake db:create
bundle exec rake db:migrate
bundle exec rake db:seed

bc. rails s

h1. Install client (sinatra app)

bc. bundle install
shotgun
